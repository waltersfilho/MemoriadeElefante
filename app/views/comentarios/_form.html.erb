<% if user_signed_in? %>
  <%= form_for([@politico, @politico.comentarios.build]) do |f| %>
        <section class="content">
          <div class="container">
            <div class="col-sm-6 padd">
              <div class="owl-carousel owl-testimonial-default">

                <div class="item">
                  <blockquote class="testimonial">
                    <p><%= f.text_area :mensagem, class: "form-control" %><br></p>
                    <%= button_tag(type: "submit", class: "btn pi-btn-light-grey", name: "commit", value: "conceal") do %>
                        Enviar comentário
                    <% end %>
                  </blockquote>
                  <div class="arrow-down"></div>
                  <div class="testimonial-info ">
                    <%=image_tag(current_user.photo.url(:thumb))%>
                  </div>
                  </div>
                </div>
              </div>
            </div>
          </section>
        <%end%>
  <% end %>