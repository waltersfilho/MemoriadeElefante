<%= form_for(@acontecimento) do |f| %>
  <% if @acontecimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acontecimento.errors.count, "error") %> prohibited this acontecimento from being saved:</h2>

      <ul>
      <% @acontecimento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
